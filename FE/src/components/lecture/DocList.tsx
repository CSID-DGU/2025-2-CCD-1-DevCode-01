import styled from "styled-components";

export const DocList = styled.ul`
  display: grid;
  gap: 0.75rem;
  padding: 0;
  margin: 0;
  list-style: none;
`;

export const ListItem = styled.li`
  position: relative;
`;
